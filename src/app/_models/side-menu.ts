export class Sidemenu {
    public name: string;
    public id: number;
    public icon: string;

    constructor(name: string, id: number, icon: string) {
        this.name = name;
        this.id = id;
        this.icon = icon;
    }
}
